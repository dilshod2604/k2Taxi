import { View, Text } from "react-native";
import React from "react";
import StartPage from "@/components/auth/page/StartPage";

const Start = () => {
  return <StartPage />;
};

export default Start;
